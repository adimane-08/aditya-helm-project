replicaCount: 1

image:
  tag: "1.0.0-dev"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 10

resources:
  limits:
    cpu: 150m
    memory: 200Mi
  requests:
    cpu: 100m
    memory: 128Mi

env:
  LOG_LEVEL: "debug"
